projection;
strict ( 2 );
use draft;
use side effects;

define behavior for Z_C_AJM_CONN alias Connection
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}